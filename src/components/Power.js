import '../App.css'

// power button should activate (and ideally visually "bite" the big kiwi pad) the effects on the sample. 
const Power = () => {
    return(
        <button className='powerButton'>🥝</button>
    )
}

export default Power